//
//  NarrativeScene.swift
//  Presentation/Scenes — SKELETON. Belum diporting.
//
//  4 layar narasi pembuka, bisa dilewati, tidak ada dinding teks.
//  Dipindah dari WWWNarrativeScene.swift (322 baris):
//    buildChrome() / updateDots() / showPanel(animated:)
//    tree(at:scale:color:tilt:) / ground()
//    panelForest() / panelRoots() / panelExchange() / panelStorm()  <- 4 panel cerita
//    touchesBegan / goToMap(_:)
//

import SpriteKit

final class NarrativeScene: SKScene {

    override func didMove(to view: SKView) {
        // TODO: port 4 panel dari WWWNarrativeScene.
    }
}
